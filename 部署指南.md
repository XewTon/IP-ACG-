# 玄策 · 免费部署指南（0 元起）

> 目标：把「前端 + 后端」完整跑起来，并拥有一个**自己的域名**，尽量不花钱。
> 本项目已为此改造完毕：`Dockerfile` / `render.yaml` / 精简依赖 `requirements-deploy.txt`，
> 后端可直接同源托管前端构建产物（一个服务 = 前端页面 + `/api` 接口，无跨域问题）。

---

## 〇、先说结论：买「xuance」这个域名要多少钱？

我帮你查了 whois，**xuance 的主流后缀都已被注册，标准价买不到**：

| 域名 | 状态 | 说明 |
|------|------|------|
| xuance.com | ❌ 已注册 | 2011 年注册，2026-10-03 到期（腾讯 DNSPod），持有人未挂售 |
| xuance.cn | ❌ 已注册 | 2010 年注册，2026-12-16 到期（易名），疑似「玄策」相关公司持有 |
| xuance.net | ❌ 已注册 | 2026-06-08 刚被注册（Gname），像是被域名投资人抢注 |
| xuance.org | ❌ 已占用 | 被某开源强化学习平台使用 |

- 想从持有人手里买 `xuance.com`：一般**几千元起步**（域名交易市场一口价），且对方未必卖，不建议。
- 如果**标准价格买得到**（指其它后缀/变体），参考价：`.com` 约 ¥80-90/年，`.cn` 约 ¥33/年（需实名），
  新顶级域 `.top / .site / .xyz / .icu / .vip` 等常有**首年 ¥1-30 促销**。

### 你可以用的「玄策」替代域名（通常都可注册）

- 拼音变体：`xuan-ce.com`、`xuance-ai.com`、`xuanceip.com`、`getxuance.com`、`xuanceops.com`
- 其它后缀：`xuance.top`、`xuance.site`、`xuance.dev`、`xuance.xyz`、`xuance.app`
- **完全免费**：`eu.org` 免费域名（如 `xuance.eu.org`，永久免费，审核需几天）；或先用平台送的子域名（`xxx.onrender.com`），以后再绑自己的

> 💡 注册渠道：国外注册商 **Cloudflare Registrar**（成本价、无加价，支持支付宝/银行卡）或 **Namecheap**（常有首年优惠）；
> 国内 **阿里云/腾讯云** 也可（`.cn` 必须实名，`.com` 也需实名认证）。

---

## 一、推荐方案：Render 免费托管 + 自定义域名（电脑可关机）

免费档：**自定义域名、自动 HTTPS、512MB 内存**都支持；缺点：闲置 15 分钟休眠、冷启动约 30-60 秒、国内访问速度一般。

### 步骤

1. **把代码推到 GitHub**（仓库已存在：`XewTon/IP-ACG-`）：

   ```bash
   git add -A
   git commit -m "deploy: 免费部署配置（Dockerfile / render.yaml / 精简依赖）"
   git push
   ```

   > 请确认 `.env`、`agent_config.json`、`zhipu_config.json`、`*.db` 未被提交（已被 `.gitignore` 排除）。

2. **注册 Render**（免费）：https://render.com → Sign up（支持 GitHub 账号直接登录）。

3. **新建 Blueprint**：Dashboard → **New + → Blueprint** → 选择 `IP-ACG-` 仓库 →
   Render 会自动读取 `render.yaml`，创建一个 `xuance` 免费 Web 服务 → **Apply**。

4. **等待构建**（首次约 5-10 分钟：装 Python 精简依赖 + 构建前端）。完成后访问：
   `https://xuance.onrender.com`（名字可能不同，以 Dashboard 显示为准）。

5. **验证**：
   - 打开首页（开机动画后是运营驾驶舱）
   - `https://xuance.onrender.com/api/health` 应返回 `{"status":"ok",...}`
   - 侧边栏「AI 运营助手」无 Key 时返回种子降级报告（功能完整可演示）

6. **绑定你自己的域名**（可选）：
   - Dashboard → 服务 `xuance` → **Settings → Custom Domains → Add**
   - 在域名注册商（或 Cloudflare DNS）添加记录：`CNAME @ → xuance.onrender.com`（或 ALIAS/A 记录，按页面提示）
   - 等 TLS 证书自动签发（几分钟），之后即可用 `https://你的域名` 访问

7. **（可选）接入大模型**：Dashboard → 服务 → **Environment** → 添加 `DASHSCOPE_API_KEY`
   （阿里云百炼免费额度申请：https://bailian.console.aliyun.com）→ 重新部署后 AI 助手走真实大模型。

---

## 二、备选方案 A：本机 + Cloudflare 免费隧道（0 注册，1 分钟上线）

适合临时演示/面试，无需任何账号，机器保持开机即可。

1. 下载 `cloudflared`：https://github.com/cloudflare/cloudflared/releases （Windows 选 `cloudflared-windows-amd64.exe`，重命名为 `cloudflared.exe`）
2. 按 `启动项目.bat` 先启动后端（8000 端口，现已自带前端静态托管，无需再开前端）：
   ```bat
   cd page-agent\backend
   .venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000
   ```
3. 开隧道：
   ```bat
   cloudflared.exe tunnel --url http://127.0.0.1:8000
   ```
4. 终端会打印 `https://xxxx.trycloudflare.com` 临时域名 —— 直接发给别人即可访问。
   > ⚠️ 临时域名每次重启会变；想要固定域名：注册 Cloudflare 账号 → Zero Trust → Tunnels → 创建隧道 →
   > 绑定你的自定义域名（需把域名 DNS 托管到 Cloudflare，免费）。

---

## 三、备选方案 B：Hugging Face Spaces（免费 Docker）

1. 注册 https://huggingface.co → 右上角 **New Space**：
   - Name：`xuance`；License：随意；SDK：**Docker**
2. 在 Space 的 **Files** 页面把仓库推上去：
   ```bash
   git remote add space https://huggingface.co/spaces/<你的用户名>/xuance
   git push space main
   ```
3. 构建完成后访问 `https://<你的用户名>-xuance.hf.space`（免费）。
   > 自定义域名需 Spaces PRO（付费），免费档用平台子域名即可。

---

## 四、注意事项

| 事项 | 说明 |
|------|------|
| 免费档休眠 | Render/HF 闲置 15 分钟会休眠，首次打开慢属正常；可装 [UptimeRobot](https://uptimerobot.com)（免费）每 10 分钟 ping 一次 `/api/health` 防休眠 |
| SQLite 数据 | 云端磁盘临时：重新部署后数据库会重置（启动时自动重新灌演示数据）。对演示/面试场景是特性；想持久化再考虑迁移到免费 PostgreSQL |
| 国内访问 | Render / HF / Cloudflare 在国内速度一般；若要求国内流畅，需用国内主机 + 域名备案（无法完全免费） |
| 密钥安全 | `DASHSCOPE_API_KEY` 等只放平台 Environment 变量，不要写进仓库 |
| MediaCrawler | 爬虫服务仅本机演示用（端口 8080），云端部署已跳过；社区页会自动提示「未连接爬虫」 |
| 大体积依赖 | playwright / chromadb / llama-index 已排除出云端依赖，采集自动降级 mock、RAG 自动降级关键词检索，功能不受影响 |

---

## 五、本地验证（部署前自检）

```bash
# 1. 构建前端（会产出 page-agent/frontend/dist）
cd page-agent/frontend && npm run build

# 2. 启动后端（自动托管 dist，访问 http://127.0.0.1:8000 即完整站点）
cd ../backend
.venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000
```

打开 http://127.0.0.1:8000 —— 与云端体验一致。
